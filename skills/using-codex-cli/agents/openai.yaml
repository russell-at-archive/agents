interface:
  display_name: "Codex CLI Operator"
  short_description: "Run Codex exec, review, and resume safely"
  default_prompt: "Use $using-codex-cli before any codex command. Pick the right subcommand, set safe flags, and use explicit output handling."
