interface:
  display_name: "GitHub CLI Operations"
  short_description: "Run gh commands safely and non-interactively"
  default_prompt: "Use $using-github-cli to execute explicit, JSON-first gh commands with clear repo targeting and verification."
