interface:
  display_name: "GitHub CLI Operations"
  short_description: "Run GitHub CLI operations non-interactively"
  default_prompt: "Use $using-github-cli to run the required gh commands with explicit flags and JSON output."
