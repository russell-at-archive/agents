interface:
  display_name: "GitLab CLI Expert"
  short_description: "Run safe, non-interactive glab workflows"
  default_prompt: "Use $using-gitlab-cli to execute GitLab CLI tasks with explicit repo scoping, deterministic flags, and JSON-first output."
